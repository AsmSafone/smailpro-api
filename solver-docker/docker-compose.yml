version: '3.8'

services:
  solver:
    build: .
    container_name: solver
    ports:
      - "9000:9000"
    environment:
      - THREADS=2
      - PAGE_COUNT=2
    restart: unless-stopped
    shm_size: '2gb'
    deploy:
      resources:
        limits:
          memory: 4G
